<%= form_for(@rocnik) do |f| %>
  <% if @rocnik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rocnik.errors.count, "error") %> prohibited this rocnik from being saved:</h2>

      <ul>
      <% @rocnik.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cislo %><br>
    <%= f.number_field :cislo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
