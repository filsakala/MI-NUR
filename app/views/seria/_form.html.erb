<%= form_for(@serium) do |f| %>
  <% if @serium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@serium.errors.count, "error") %> prohibited this serium from being saved:</h2>

      <ul>
      <% @serium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nazov %><br>
    <%= f.text_field :nazov %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
